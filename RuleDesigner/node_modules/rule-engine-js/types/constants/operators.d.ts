export namespace OPERATOR_NAMES {
    let EQ: string;
    let NEQ: string;
    let GT: string;
    let GTE: string;
    let LT: string;
    let LTE: string;
    let IN: string;
    let NOT_IN: string;
    let AND: string;
    let OR: string;
    let NOT: string;
    let CONTAINS: string;
    let STARTS_WITH: string;
    let ENDS_WITH: string;
    let REGEX: string;
    let BETWEEN: string;
    let IS_NULL: string;
    let IS_NOT_NULL: string;
}
export namespace STATE_OPERATOR_NAMES {
    let CHANGED: string;
    let CHANGED_BY: string;
    let CHANGED_FROM: string;
    let CHANGED_TO: string;
    let INCREASED: string;
    let DECREASED: string;
}
export namespace DEFAULT_CONFIG {
    let maxDepth: number;
    let maxOperators: number;
    let maxCacheSize: number;
    let enableCache: boolean;
    let enableDebug: boolean;
    let strict: boolean;
    let allowPrototypeAccess: boolean;
}
